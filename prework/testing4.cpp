#include <iostream>
#include <vector>
#include <string>
#include <fstream>

using namespace std;

int main(){

  variable = (condition) ? true : false

  int largestNum = (5>2) ? 5 : 2;

  return 0;
}
